apiVersion: v1
kind: Service
metadata:
  name: {{ include "ark-cluster.fullname" . }}
  labels:
    {{- include "ark-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ark-cluster.selectorLabels" . | nindent 4 }}
