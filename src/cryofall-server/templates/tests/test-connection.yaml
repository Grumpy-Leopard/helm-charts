apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cryofall-server.fullname" . }}-test-connection"
  labels:
    {{- include "cryofall-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cryofall-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
